<UserControl
    x:Class="TestCaliburnMicro.Views.SectionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TestCaliburnMicro.Views"
    d:DataContext="{d:DesignInstance Type=viewModels:SectionViewModel, IsDesignTimeCreatable=True}"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:TestCaliburnMicro.ViewModels"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    >
    
    <Grid Style="{StaticResource Level3Style}">
        <TextBlock Style="{StaticResource H1Style}" Text="{Binding DisplayName}" />
    </Grid>
</UserControl>
